public class Income extends Transaction
{
    //main constructor inheriting form the Transaction class
    public Income(String description, float totalValue)
    {
        super(description, totalValue);
    }
}